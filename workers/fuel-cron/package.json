{
  "name": "gbdriver-fuel-cron",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create gbdriver-fuel-history",
    "db:migrate": "wrangler d1 migrations apply gbdriver-fuel-history --remote",
    "db:migrate:local": "wrangler d1 migrations apply gbdriver-fuel-history"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "wrangler": "^4.0.0"
  }
}
